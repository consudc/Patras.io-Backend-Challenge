const { assert } = require("chai");
const { expect } = require("chai");
const should = require("chai").should();

const {
  getAeropuerto,
  addAeropuerto,
} = require("../../src/controllers/aeropuertos");
